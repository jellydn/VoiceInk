import SwiftUI
import AppKit

struct DashboardPromotionsSection: View {
    // Remove all license state dependency for free fork
    var body: some View {
        // Remove all promotions for free fork version
        EmptyView()
    }
}